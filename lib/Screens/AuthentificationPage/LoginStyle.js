import { StyleSheet } from "react-native";

const LoginStyle = StyleSheet.create({
    textInputStyle: {
        width: 200,
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
    },
});
export default LoginStyle